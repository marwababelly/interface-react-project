import React from "react";
import classes from './Nav.module.css';

const Nav = () => {
    return (
            <nav className={classes.nav}>
                <h2 className={classes.text}>React Interface!</h2>
            </nav>
    );
};

export default Nav;